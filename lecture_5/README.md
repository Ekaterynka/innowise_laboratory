Simple Book Collection API

A REST API for managing personal book collection built with FastAPI and SQLAlchemy ORM.

Installation and Setup

Requirements:

-Python 3.8 or higher
-pip package manager

Steps to run: 

1. Clone the repository

2. Install dependencies:
    pip install -r requirements.txt

3. Start the application: uvicorn main:
    app --reload
